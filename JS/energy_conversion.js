 var frequency = 0;
var seconds = 0;
var speed_of_light = 299792458;
var wavelength_ft = 1100 / frequency;
var wavelength_m = 335 / frequency;
var meters = 0;

var bass = 0;
var midrange = 0;
var seconds_to_degree = 0;
var treble = 0;
var degrees_of_cycle = 0;
var	seconds_to_degree = frequency / ( degrees_of_cycle / 360 );
var	pk_current = 1.414 * voltage;
var 	pk_voltage = 1.414 * current;
var	pk_root_mean_square = voltage * .707;
var	pk_pk_root_mean_square = voltage * .354;
var	time = 0;
var frequency = frequency / seconds;
var	cycle_degrees = 360;
var	pi = 3.1416;
var	radians = 2 * pi * frequency;
var	power = voltage * current;
var	power = (current * resistance) * resistance;
var	power = (current * current ) * resistance;
var	power = voltage * (voltage / resistance);
var	power = ( voltage * voltage ) / resistance;
var	voltage = current * resistance;
var	current = voltage / resistance;
var	resistance = voltage / current;
var range;

//int main() 
//
//	float power = 0, current = 0, voltage = 0, resistance = 0; 
//char choice = 0;
//
//	var light_distance = speed_of_light * seconds;
//    var speed_of_light / seconds = meters;
//	wavelength_ft = 1100 / frequency;
//	wavelength_m = 335 / frequency;
 
//function frequency_conversion(frequency) { 
//	if (frequency >=20 && frequency <= 200 )
//		alert("frequency = bass");
//	if else (frequency >= 200 && frequency <= 3000)
//	alert("frequency = midrange");
//	if else (frequency >= 3000)
//	alert("frequency = treble");
//    else 
//        alert ("out of range");
//        
//        }
//function frequency_conversion() {
//    var frequency = document.getElementById("frequncy_conversions").value;
//    // note: using the same variable for input AND output is not good practice
//    if (frequency >= 20 && frequency <= 200) frequency = "bass";
//    else if (frequency >= 200 && frequency <= 3000) frequency = "midrange";
//    else if (frequency >= 3000) frequency = "treble";
//    else range = "out of range";
//    document.getElementById("range").innerHTML = frequency;
//    alert('range of frequency is:' + frequency + '');
//}
//
//    function distance_light() {
//        var seconds = document.getElementById("light_coordinate").value
// return  var dist_coordinate = speed_of_light * seconds;
//        alert(dist_coordinate);
//}

// var dist_coordinate = speed_of_light * seconds;

 var frequency = 0;
var seconds;
var speed_of_light = 299792458;
var wavelength_ft = 1100 / frequency;
var wavelength_m = 335 / frequency;
var meters = 0;

var bass = 0;
var midrange = 0;
var seconds_to_degree = 0;
var treble = 0;
var degrees_of_cycle = 0;
var	seconds_to_degree = frequency / ( degrees_of_cycle / 360 );
var	pk_current = 1.414 * voltage;
var 	pk_voltage = 1.414 * current;
var	pk_root_mean_square = voltage * .707;
var	pk_pk_root_mean_square = voltage * .354;
var	time = 0;
var frequency = frequency / seconds;
var	cycle_degrees = 360;
var	pi = 3.1416;
var	radians = 2 * pi * frequency;
var	power = voltage * current;
var	power = (current * resistance) * resistance;
var	power = (current * current ) * resistance;
var	power = voltage * (voltage / resistance);
var	power = ( voltage * voltage ) / resistance;
var	voltage = current * resistance;
var	current = voltage / resistance;
var	resistance = voltage / current;
var range;

//int main() 
//
//	float power = 0, current = 0, voltage = 0, resistance = 0; 
//char choice = 0;
//
//	var light_distance = speed_of_light * seconds;
//    var speed_of_light / seconds = meters;
//	wavelength_ft = 1100 / frequency;
//	wavelength_m = 335 / frequency;
 
//function frequency_conversion(frequency) { 
//	if (frequency >=20 && frequency <= 200 )
//		alert("frequency = bass");
//	if else (frequency >= 200 && frequency <= 3000)
//	alert("frequency = midrange");
//	if else (frequency >= 3000)
//	alert("frequency = treble");
//    else 
//        alert ("out of range");
//        
//        }
//function frequency_conversion() {
//    var frequency = document.getElementById("frequncy_conversions").value;
//    // note: using the same variable for input AND output is not good practice
//    if (frequency >= 20 && frequency <= 200) frequency = "bass";
//    else if (frequency >= 200 && frequency <= 3000) frequency = "midrange";
//    else if (frequency >= 3000) frequency = "treble";
//    else range = "out of range";
//    document.getElementById("range").innerHTML = frequency;
//    alert('range of frequency is:' + frequency + '');
//}
//
//    function distance_light() {
//        var seconds = document.getElementById("light_coordinate").value
// return  var dist_coordinate = speed_of_light * seconds;
//        alert(dist_coordinate);
//}

// var dist_coordinate = speed_of_light * seconds;

 var frequency = 0;
var seconds;
var speed_of_light = 299792458;
var wavelength_ft = 1100 / frequency;
var wavelength_m = 335 / frequency;
var meters = 0;

var bass = 0;
var midrange = 0;
var seconds_to_degree = 0;
var treble = 0;
var degrees_of_cycle = 0;
var	seconds_to_degree = frequency / ( degrees_of_cycle / 360 );
var	pk_current = 1.414 * voltage;
var 	pk_voltage = 1.414 * current;
var	pk_root_mean_square = voltage * .707;
var	pk_pk_root_mean_square = voltage * .354;
var	time = 0;
var frequency = frequency / seconds;
var	cycle_degrees = 360;
var	pi = 3.1416;
var	radians = 2 * pi * frequency;
var	power = voltage * current;
var	power = (current * resistance) * resistance;
var	power = (current * current ) * resistance;
var	power = voltage * (voltage / resistance);
var	power = ( voltage * voltage ) / resistance;
var	voltage = current * resistance;
var	current = voltage / resistance;
var	resistance = voltage / current;
var range;

//int main() 
//
//	float power = 0, current = 0, voltage = 0, resistance = 0; 
//char choice = 0;
//
//	var light_distance = speed_of_light * seconds;
//    var speed_of_light / seconds = meters;
//	wavelength_ft = 1100 / frequency;
//	wavelength_m = 335 / frequency;
 
//function frequency_conversion(frequency) { 
//	if (frequency >=20 && frequency <= 200 )
//		alert("frequency = bass");
//	if else (frequency >= 200 && frequency <= 3000)
//	alert("frequency = midrange");
//	if else (frequency >= 3000)
//	alert("frequency = treble");
//    else 
//        alert ("out of range");
//        
//        }
//function frequency_conversion() {
//    var frequency = document.getElementById("frequncy_conversions").value;
//    // note: using the same variable for input AND output is not good practice
//    if (frequency >= 20 && frequency <= 200) frequency = "bass";
//    else if (frequency >= 200 && frequency <= 3000) frequency = "midrange";
//    else if (frequency >= 3000) frequency = "treble";
//    else range = "out of range";
//    document.getElementById("range").innerHTML = frequency;
//    alert('range of frequency is:' + frequency + '');
//}
//
//    function distance_light() {
//        var seconds = document.getElementById("light_coordinate").value
// return  var dist_coordinate = speed_of_light * seconds;
//        alert(dist_coordinate);
//}

// var dist_coordinate = speed_of_light * seconds;

function frequency_conversion() {
  var frequency = document.getElementById("calculator").value;
  // note: using the same variable for input AND output is not good practice
  if (frequency >= 20 && frequency <= 200) frequency = "bass";
  else if (frequency >= 200 && frequency <= 3000) frequency = "midrange";
  else if (frequency >= 3000) frequency = "treble";
  else range = "out of range";
  // document.getElementById("range").innerHTML = frequency;
  alert('range of frequency is:' + frequency + '');
}

  function distance_light() {
    var seconds = document.getElementById("seconds").value;
    alert('The distance the light  has traveled is:' + speed_of_light * seconds + ' meters');
    }
 function frequency_to_wavelength() {
    var frequenc = document.getElementById("freq").value;
    alert('The wavelength with the given frequency is:' + speed_of_light / frequenc + 'meters a second');
    }

function wavelength_to_frequency() {
    var wavlgnth = document.getElementById("wavlng").value;
    alert('The frequency with the given wavelength is:' + speed_of_light / wavlgnth + '');
    }

function powerCnR () {
 var current = document.getElementById("current").value;
  var resistance = document.getElementById("resistance").value;
   alert('the power is ' + (current * resistance) * resistance + ''); 
}

function powerVnC () {
  var voltage = document.getElementById("volt").value;
  var current = document.getElementById("curre").value;
  alert('the power is ' + voltage * current + '');
}
//h2{ input = frequency;
//button = prompt("Your frequency range = + frequency_conversion +";
}
//	seconds_to_degree = frequency / ( degrees_of_cycle / 360 ); 
//	pk_current = 1.414 * voltage;
//	pk_voltage = 1.414 * current;
//	pk_root_mean_square = voltage * .707;
//	pk_pk_root_mean_square = voltage * .354;
//	time = 0;
//	frequency = 1/time;
//	cycle_degrees = 360;
//	pi = 3.1416;
//	radians = 2 * pi * frequency;
//	power = voltage * current;
//	power = (current * resistance) * resistance;
//	power = (current * current ) * resistance;
//	power = voltage * (voltage / resistance);
//	power = ( voltage * voltage ) / resistance;
//	voltage = current * resistance;
//	current = voltage / resistance;
//	resistance = voltage / current;

//printf("Enter your choice:\n1 if you want to calculate power using voltage and current.\n 2 if you want to calculate power using current and resistance.\n 3 if you want to calculate power using voltage and resistance.\n 4 if you want to calculate voltage using current and resistance.\n 5 if you want to calculate current using voltage and resistance.\n 6 if you want to calculate resistance using voltage and current. ");
//scanf_s("%d", &choice);
//		
 //if (choice = 1)
/*else {printf("Enter your choice:\n1 if you want to calculate power using voltage and current.\n 2 if you want to calculate power using current and resistance.\n 3 if you want to calculate power using voltage and resistance.\n 4 if you want to calculate voltage using current and resistance.\n 5 if you want to calculate current using voltage and resistance.\n 6 if you want to calculate resistance using voltage and current. ");
scanf_s("%d", &choice);}*/
//	 do {
//printf( "You have chosen to calculate power\n using voltage & current,\n please enter the voltage:");
//	scanf_s("%f", &voltage);
//	printf("please enter the current:");
//		scanf_s("%f", &current);
//	power = voltage * current;
//		printf("%f", power);
//	 }
//	 while (choice = 1);

		//return system("pause");
// if (choice = 2 )
//	 do  {
//	printf("You have chosen to calculate power using current and resistance, please enter the current:");
//	scanf_s("%f", &current);
//	printf_s("then the resistance:");
//	scanf_s("%f", &resistance);
//	power = (current * current ) * resistance;
//	printf("%f",power);
//	 }
//	 while (choice = 2);
//	//return system("pause");
//// if (choice = 3 )
//	 do{
//	printf("You have chosen to calculate power using voltage and resistance, please enter the voltage:");
//	scanf("%f", &voltage);
//	printf("please enter the resistance:");
//	scanf_s("%f",&resistance);
//	power = ( voltage * voltage ) / resistance;
//	printf("%f", power);
//	 }
//	 while (choice = 3);
//	return system("pause");

	//if (choice == 4)
//   }